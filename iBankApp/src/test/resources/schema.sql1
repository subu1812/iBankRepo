CREATE TABLE ACCOUNTS (ACCOUNT_ID NUMBER PRIMARY KEY, ACCOUNT_NAME VARCHAR2(200),PHONE NUMBER, EMAIL VARCHAR2(400),STATUS VARCHAR2(400));
CREATE TABLE ACCOUNT_BALANCE (ACCOUNT_ID NUMBER PRIMARY KEY, BALANCE NUMBER)
